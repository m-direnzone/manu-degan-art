import Layout from "../Layout";
import ContactsPage from "../pages/ContactsPage";

const Contacts = () => {
  return (
    <Layout>
      <ContactsPage />
    </Layout>
  );
};

export default Contacts;
